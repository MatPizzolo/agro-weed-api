# URI del modelo a servir: ruta local o URI de MLflow (models:/weed-classifier/Production)
MODEL_URI=models/weed_classifier.keras

# Tracking de MLflow
MLFLOW_TRACKING_URI=http://localhost:5000

# Orígenes permitidos para CORS (separados por coma)
CORS_ORIGINS=http://localhost:5173
